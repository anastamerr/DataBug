# Backend
# Copy this file to `backend/.env` (gitignored) and fill in values.

DATABASE_URL=
# Optional: Alembic-only DB URL override (useful for Supabase Session Pooler).
ALEMBIC_DATABASE_URL=
REDIS_URL=redis://redis:6379/0
OLLAMA_HOST=http://ollama:11434
OLLAMA_MODEL=llama3:8b

# LLM (cloud via OpenRouter; recommended if you can't run Ollama locally)
# If OPEN_ROUTER_API_KEY is set, backend will use OpenRouter by default.
LLM_PROVIDER=auto
OPEN_ROUTER_API_KEY=
OPEN_ROUTER_MODEL=meta-llama/llama-3.1-8b-instruct
OPEN_ROUTER_BASE_URL=https://openrouter.ai/api/v1
OPEN_ROUTER_SITE_URL=
OPEN_ROUTER_APP_NAME=scanguard-ai

# Pinecone
PINECONE_API_KEY=
PINECONE_ENVIRONMENT=

# GitHub (real bug ingestion)
GITHUB_TOKEN=
GITHUB_WEBHOOK_SECRET=
GITHUB_REPOS=owner/repo1,owner/repo2
GITHUB_BACKFILL_LIMIT=50
GITHUB_BACKFILL_ON_START=false

# Supabase Auth (JWT verification)
SUPABASE_JWT_SECRET=
SUPABASE_JWT_ISSUER=

# Nuclei (DAST)
NUCLEI_TEMPLATES_PATH=
NUCLEI_TIMEOUT_SECONDS=300
NUCLEI_RATE_LIMIT=
NUCLEI_SEVERITIES=critical,high,medium,low
